function controler() {
    var zoneSaisie = document.getElementById("zoneSaisie");
    alert("La Zone de saisie contient : " + zoneSaisie.value);
}

/* == Décrivez le code de cette fonction == */
/*
Cette fonction cible l'input (ou champ) portant l'identifiant ID 'zoneSaisie'
et renvoie sa valeur en alerte à côté du texte ' "La Zone de saisie contient : " '
*/

/* == Il faudrait maintenant associer ce code de controle à l'évenement == */
zoneSaisie.clic


/*
*/


